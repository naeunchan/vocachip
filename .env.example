# Copy to .env for local work or release preparation.
# Production builds should use APP_ENV=production.
APP_ENV=development

# Apps in Toss identity
# `AIT_APP_NAME` must match the app name registered in the Apps in Toss console.
AIT_APP_NAME=vocachip
AIT_DISPLAY_NAME=Vocachip
AIT_APP_ICON_URL=https://example.com/vocachip/icon.png
AIT_PRIMARY_COLOR=#1d4ed8

# Hosted legal URLs used by the Apps in Toss runtime.
VOCACHIP_PRIVACY_POLICY_URL=https://vocachip.app/legal/privacy
VOCACHIP_TERMS_OF_SERVICE_URL=https://vocachip.app/legal/terms

# Client runtime
# Production defaults disable account auth unless this is explicitly set to true.
VOCACHIP_FEATURE_ACCOUNT_AUTH=true
VOCACHIP_FEATURE_GUEST_ACCOUNT_CTA=false
VOCACHIP_FEATURE_BACKUP_RESTORE=false
VOCACHIP_FEATURE_REVIEW_LOOP=false
VOCACHIP_FEATURE_REVIEW_HOME_DASHBOARD=false
VOCACHIP_FEATURE_REVIEW_SESSION_UI=false
VOCACHIP_FEATURE_DAILY_GOAL=false
VOCACHIP_FEATURE_REVIEW_REMINDER=false
VOCACHIP_FEATURE_COLLECTIONS=false
VOCACHIP_FEATURE_FAVORITES_BATCH_ACTIONS=false
VOCACHIP_FEATURE_AI_STUDY_MODE=false
VOCACHIP_FEATURE_AI_STUDY_ENTRY_POINTS=false
VOCACHIP_FEATURE_AI_STUDY_SESSION_UI=false

# AI proxy
VOCACHIP_OPENAI_PROXY_URL=https://api.example.com
VOCACHIP_OPENAI_PROXY_KEY=replace-with-client-proxy-key
VOCACHIP_AI_HEALTH_URL=https://api.example.com/health
VOCACHIP_SENTRY_DSN=
VOCACHIP_DICTIONARY_PROXY_URL=
AI_METRICS_URL=https://api.example.com/metrics
AI_METRICS_TIMEOUT_MS=5000
# GitHub Actions monitor can reuse `AI_PROXY_KEY` to read `/metrics`.
# Optionally store route threshold overrides in a repository variable:
# AI_METRICS_THRESHOLDS_JSON={"\/dictionary\/tts":{"maxUpstreamAverageMs":1800}}
# Optional JSON override for route-specific alert thresholds.
# AI_METRICS_THRESHOLDS_JSON={"\/dictionary\/examples":{"maxUpstreamAverageMs":2000}}

# Server runtime
NODE_ENV=development
PORT=4000
AI_PROXY_KEY=replace-with-server-shared-secret
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=alloy
OPENAI_TTS_FORMAT=mp3
AI_HEALTH_DEGRADED_WINDOW_MS=600000

# Optional explicit browser origins for the AI proxy.
# When `AIT_APP_NAME` is set, the server also allows:
#   https://<AIT_APP_NAME>.apps.tossmini.com
#   https://<AIT_APP_NAME>.private-apps.tossmini.com
CORS_ORIGINS=http://localhost:8081,http://localhost:19006
